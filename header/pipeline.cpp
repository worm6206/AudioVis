#include <fstream>
#include <vector>
#include <string>
#include "pipeline.hpp"

using namespace std;

vector<string> pipelineReader::readFile(string fileName){
	
	vector<string> output;
	return output;
}